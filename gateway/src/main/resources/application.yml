server:
  port: 8888

spring:
  application:
    name: Gateway
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: Gateway
    discovery:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
